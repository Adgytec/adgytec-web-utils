# adgytec-web-utils

`adgytec-web-utils` is a lightweight, framework-agnostic utility library for Adgytec web applications.

## Purpose

This library exists to provide **shared, reusable utilities** that can be consumed across multiple Adgytec web apps.

